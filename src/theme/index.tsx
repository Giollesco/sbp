import { MantineTheme } from "@mantine/core";

export const theme: Partial<MantineTheme> = {
    fontFamily: 'Poppins',
    loader: 'bars',
    defaultRadius: 'md',
    // primaryColor: 'yellow',
    // colorScheme: 'dark'
}